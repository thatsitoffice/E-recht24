# Cloudflare Workers/Pages Konfiguration
# Wird für Cloudflare Pages verwendet

name = "erecht-generator"
compatibility_date = "2024-01-01"

# Für Production Environment
[env.production]
vars = { NODE_ENV = "production" }

# Build-Konfiguration
[build]
command = "npm run build"
cwd = "."
